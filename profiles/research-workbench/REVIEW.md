# 合并前代码审查

审查日期：2026-09-26。范围为[验收记录](VALIDATION.md)中的10个关联PR；检查新增执行路径、数据与时间语义、实验完整性、依赖固定方式、回归测试及GitHub检查。这是本次实现后的代码审查，没有声称第三方独立审计。

## 已复现并修复的问题

|级别|问题及影响|根因修复与回归证据|
|---|---|---|
|P1|带缓存pe_ratio的行情配合无关历史文件，可以运行pe_inv，绕过因子对应的披露时间要求|逐个检查PIT因子所需字段必须声明fundamentals映射；原先应拒绝的案例确实成功运行，修复后拒绝；正确披露历史仍成功运行|
|P2|观察池外证券的受限状态会阻断整个研究|调仓检查限定到当前观察池；原先带OTHER=false的完整历史导致回放异常，修复后当前池正常运行|
|P2|重试成功后可删去摘要中的早期失败记录、修改顺序或时间而通过报告校验|报告逐条比对SQLite事件的attempt_id、状态、时间与payload；三种篡改均在修复前漏检，修复后拒绝|

新增6项回归测试。a-share-multifactor完整测试152项通过、覆盖率85.13%；quant-report-hub完整测试108项通过、4项既有跳过，覆盖率87.52%。Ruff通过。未放宽原有测试、完整性检查或覆盖率门槛。

## 依赖与合并约束

修复提交同步固定到quant-pipeline的research依赖及本目录stack.json。公共包依赖没有变化。期货/crypto继续使用原冻结环境，三个认证依赖SHA没有变化。

各PR以实际审查过的head SHA执行合并，要求最新检查成功且无冲突；采用merge commit保留被依赖清单引用的提交，不改写历史、不删除依赖分支、不绕过保护规则。合并后核验原提交属于默认分支历史。

## 剩余边界

本次检查未将短期真实观察池、合成ETF或冻结fixture升级为全市场策略验证。真实多年历史、动态成分/受限状态执行模型、独立样本外评价及在线LLM质量仍受[验收记录](VALIDATION.md)所列范围约束。这些边界没有通过兜底数据或收益筛选被隐藏。
